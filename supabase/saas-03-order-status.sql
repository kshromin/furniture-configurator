-- ============================================================================
-- SaaS: статус заказа — папки «Активные / Завершённые / Отменённые»
-- Задание «поделиться 29,07» («чтобы не хламить заказы»). Выполнять в Supabase →
-- SQL Editor после saas-01. Аддитивно: одна колонка.
-- ============================================================================

alter table public.projects
  add column if not exists status text not null default 'active';
-- status: 'active' | 'completed' | 'cancelled'. Используется у заказов (kind='order');
-- у проектов не задействован. RLS не трогаем — доступ тот же (владелец/расшаренный/админ),
-- папки это просто клиентский фильтр по status.
